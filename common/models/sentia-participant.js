var _ = require('lodash');
var utils = require('./utils');
var log = require('bunyan').createLogger({name: "SentiaParticipant"});
var webError = {
  success: false,
  msg: "Something went wrong"
}

module.exports = function(SentiaParticipant) {
}